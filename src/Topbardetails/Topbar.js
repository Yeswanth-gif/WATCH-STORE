import React from 'react' 
import classes from './Topbar.module.css'

const Topbar = () => {
    return (
        <nav className={classes.Topbar}>
          <h1 className={classes.head}>WATCH STORE</h1>
        </nav>
    )

}

export default Topbar;